#!/bin/sh

#git prompt by zkwinkle // 28-05-25
branch=""
action=""
if [ "$(git rev-parse --is-inside-work-tree 2>/dev/null)" = "true" ]; then
  branch="[$(git branch --show-current)] "
  # action="%F{14}(%a) "
fi
echo "$branch$action"
